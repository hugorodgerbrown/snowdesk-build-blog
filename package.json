{
  "name": "snowdesk-build-blog",
  "version": "1.0.0",
  "private": true,
  "description": "How we built Snowdesk — the static blog served at build.snowdesk.info",
  "type": "module",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "build": "eleventy",
    "build:drafts": "BUILD_DRAFTS=true eleventy",
    "start": "BUILD_DRAFTS=true eleventy --serve",
    "clean": "rm -rf _site",
    "lint:html": "html-validate \"_site/**/*.html\"",
    "lint:links": "linkinator _site --recurse --skip \"^https?://(?!localhost)\"",
    "check": "npm run clean && npm run build:drafts && npm run lint:html && npm run lint:links",
    "lighthouse": "npm run clean && npm run build && npx --yes @lhci/cli@0.15.1 autorun"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.6",
    "@11ty/eleventy-plugin-rss": "3.0.0",
    "html-validate": "10.17.0",
    "linkinator": "7.6.1"
  }
}
